version: 2.1
jobs:
  beta-job:
    docker:
      - image: cimg/android:2025.08
    steps:
      - checkout
      - run: bash scripts/prepare.sh && bash scripts/deploy.sh

workflows:
  beta-workflow:
    jobs:
      - beta-job